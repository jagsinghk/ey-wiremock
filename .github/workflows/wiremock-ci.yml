name: WireMock CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  wiremock:
    runs-on: ubuntu-latest  

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run API tests in Docker
        run: docker compose up --abort-on-container-exit --exit-code-from api-tests api-tests

      - name: Tear down after API tests
        if: always()
        run: docker compose down

      - name: Run performance tests in Docker
        run: docker compose up --abort-on-container-exit --exit-code-from perf-tests perf-tests

      - name: Publish API test report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-test-report
          path: build/reports/tests/test/

      - name: Publish Gatling performance report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: gatling-report
          path: build/reports/gatling/

      - name: Print WireMock logs on failure
        if: failure()
        run: docker compose logs

      - name: Tear down
        if: always()
        run: docker compose down
